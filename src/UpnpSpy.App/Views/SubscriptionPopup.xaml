<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="UpnpSpy.App.Views.SubscriptionPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:UpnpSpy.Core.ViewModels"
    xmlns:models="using:UpnpSpy.Core.Models"
    Title="Subscribe">
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   FontSize="18"
                   FontWeight="SemiBold" />

        <InfoBar x:Name="StatusBar"
                 Grid.Row="1"
                 IsOpen="True"
                 IsClosable="False"
                 Severity="Informational"
                 Title="Subscription pending"
                 Message="Waiting for the device to confirm…" />

        <TextBlock x:Name="CallbackText"
                   Grid.Row="2"
                   FontFamily="Consolas"
                   Foreground="{ThemeResource SystemBaseMediumColor}" />

        <Expander Grid.Row="3"
                  Header="Latest property values"
                  IsExpanded="True">
            <ItemsControl x:Name="LatestList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:EventPropertyRow">
                        <Grid ColumnSpacing="8" Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{x:Bind Name}"
                                       FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1"
                                       Text="{x:Bind Value, Mode=OneWay}"
                                       TextWrapping="Wrap"
                                       IsTextSelectionEnabled="True" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Expander>

        <ListView x:Name="EventsList"
                  Grid.Row="4"
                  SelectionMode="None"
                  IsItemClickEnabled="False">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:EventNotification">
                    <StackPanel Orientation="Vertical"
                                Padding="0,4,0,4"
                                Spacing="2">
                        <TextBlock FontFamily="Consolas">
                            <Run Text="SEQ " />
                            <Run Text="{x:Bind SequenceNumber}" />
                            <Run Text="  " />
                            <Run Text="{x:Bind ReceivedUtc}" />
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
